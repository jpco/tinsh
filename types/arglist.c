

// local includes
#include "m_str.h"
#include "linkedlist.h"

// self-include
#include "arglist.h"

arglist_j *al_make (m_str *list)
{
/*        char *cm;
        while ((cm = ms_strchr(list, ',')) != NULL) {
                // for arg in arglist
                m_str *arg = 
        } */
        // last arg
        return NULL;
}
