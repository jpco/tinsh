#!/home/jpco/bin/jpsh

echo "no fns loaded yet"

fn mvmany - srcli ... , dest {
    for src in (srcli) {
        if -e (src): mv src dest
    }
}

echo "fn loaded now"
mvmany foo bar baz ..

with root_dir = `su -i & pwd` {
    if (root_dir) == '': break

    fn getrootlibs - dirs ... {
        for dir in (dirs) {
            
        }
    }
}
